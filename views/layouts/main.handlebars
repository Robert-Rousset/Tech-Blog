<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Tech Blog</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css" />
    <link rel="preconnect" href="https://fonts.gstatic.com" />
    <link rel="shortcut icon" href="/images/favicon.png" type="image/x-icon">
    <link
      href="https://fonts.googleapis.com/css2?family=Lato:wght@300&family=Odibee+Sans&family=Quicksand:wght@700&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <header>
      <h1>Tech-Blog</h1>
      <nav>
        <a class="navbarButton" href="/">Home</a>
        <a class="navbarButton" href="/dashboard">Dashboard</a>
        {{#if loggedIn}}
          <a class="navbarButton" id="logout" href="">Logout</a>
        {{else}}
          <a class="navbarButton" href="/login">Login</a>
        {{/if}}

      </nav>
    </header>

    <main>
      {{{body}}}
    </main>
    {{#if loggedIn}}
      <script src="/js/logout.js"></script>
    {{/if}}
  </body>
  <div class="space"></div>
</html>